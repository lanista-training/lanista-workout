// @ts-ignore

// Export the `styled-component` lib with theme definitions

// ----------------------------------------------------------------------------
// IMPORTS

/* NPM */
import { ComponentClass } from "react";
import styled from "styled-components";
import { injectGlobal, css, keyframes, ThemeProvider } from "styled-components";

// ----------------------------------------------------------------------------

//const css = styledComponents.css;
//const injectGlobal = styledComponents.injectGlobal;
//const keyframes = styledComponents.keyframes;
//const ThemeProvider = styledComponents.ThemeProvider;

export {
  css,
  keyframes,
  injectGlobal,
  ThemeProvider,
};

export default styled;
