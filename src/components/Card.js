import React, { Component } from 'react';
import { Container, Header, Content, Card, CardItem, Body, Text } from 'native-base';

class LanistaCard extends Card {

  render() {
    return (
      <Card {...this.props}>
      
      </Card>
    );
  }

}

export {LanistaCard as Card};
